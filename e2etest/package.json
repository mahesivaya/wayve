{
  "name": "rwayve-e2etest",
  "private": true,
  "version": "0.1.0",
  "description": "End-to-end Playwright suite for the rwayve app (runs against the local docker stack).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
